doAnswer(invocation -> {
            ((io.vertx.core.Handler<io.vertx.core.AsyncResult<JsonArray>>) invocation.getArguments()[5])
                    .handle(Future.succeededFuture(null)); // Simulate null result
            return null;
        }).when(transactionalReferenceDataService)
                .executePreparedQueryList(anyString(), any(JsonArray.class), anyString(), anyInt(), anyString(), any(Marker.class), any());
